"""
The `my-gemini-agent` package.

This package contains the definition and configuration for the Gemini AI Agent.
It exposes the `agent` module which defines the `root_agent`.
"""
from . import agent
